[pytest]
markers = 
    slow:
        "Mark test as slow to run"
    integration:
        "Mark test as integration test"
addopts = --cov=app --cov-report=html --cov-report=xml --junitxml=test-results.xml